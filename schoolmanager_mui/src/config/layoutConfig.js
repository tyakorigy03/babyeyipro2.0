/**
 * Structural placeholder only — no widgets, routing, or domain wiring.
 *
 * Sections are arrays consumed by `/core/layout-engine` (never rendered here).
 */

export const layoutConfig = Object.freeze({
  sidebar: [],
  topbar: [],
  widgets: [],
});
